
"Button" : {
	height				: 35,
	top					: 15,
	left				: 15,
	right				: 15
}

"TextField" : {
	top						: 5,
	left					: 15,
	right 					: 15
}

".container" : {
 	height 					: Ti.UI.SIZE,
	left					: 0,
	right					: 0,
	layout 					: 'vertical'
}

"#username" : {
	autocapitalization	: Ti.UI.TEXT_AUTOCAPITALIZATION_NONE,
	keyboardType		: Ti.UI.KEYBOARD_EMAIL,
	top					: 15,
	left				: 15,
	right				: 15
}
